image:
  repository: "opensearchplayground/opensearch-dashboards"
  tag: "2.7.0-fix"
  pullPolicy: "IfNotPresent"

replicaCount: 4
updateStrategy:
  type: "RollingUpdate"
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0

# TODO: bind to the actual host
#ingress:
#  enabled: true
#  ingressClassName: alb
#  annotations:
#    alb.ingress.kubernetes.io/scheme: internet-facing
#      # WARNING: HTTPS should be enabled for production deployment
#    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
#    alb.ingress.kubernetes.io/target-type: ip
#  labels: {}
#  hosts:
#    - paths:
#        - path: /
#          backend: {}

ingress:
  enabled: true
  ingressClassName: alb
  annotations:
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}, {"HTTP": 80}]'
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    #alb.ingress.kubernetes.io/security-groups: sg-0a82a4cba71dca0db
    #alb.ingress.kubernetes.io/manage-backend-security-group-rules: 'true'
  labels: {}
  hosts:
    - host: ml.playground.opensearch.org
      paths:
        - path: /
          backend: {}

service:
  type: NodePort
